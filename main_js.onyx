// this file is used for playground (in JS target, core.os cannot be used)

use core {println}

#load "./src/lox/lox"

#export "runLox" run_lox
run_lox :: (source: str) -> void {
    lox := &Lox.{};
    lox->run(source);
}

main :: (args) => {
    // dummy
}
